package com.lge.sureparksystem.parkserver.manager.keyinmanager;

public class KeyInCorpus {
	public static final String[] ScanReservationCode = {
		"Scan Reservation Code",
		"Scan your reservation code"
	};
	
	public static final String[] WelcomeSurePark = {
		"welcome surepark",
		"welcome sure park",
		"welcome sure park system"
	};
	
	public static final String[] AssignedSlot = {
		"Assigned Slot",
		"assign slot"
	};

	public static final String[] OpenEntryGate = {
		"Open Entry Gate",
		"Open the entry gate",
		"Raise the entry servo",
		"up the entry servo",
	};

	public static final String[] CloseEntryGate = {
		"Close Entry Gate",
		"Close the entry gate",
		"Down the entry servo",
	};

	public static final String[] OpenExitGate = {
		"Open Exit Gate",
		"Open the Exit gate",
		"Raise the Exit servo",
		"up the Exit servo",
	};

	public static final String[] CloseExitGate = {
		"Close Exit Gate",
		"Close the Exit gate",
		"Down the Exit servo",
	};

	public static final String[] TurnOnEntryGateLED = {
		"Turn On Entry Gate LED",
		"Turn On EntryGate LED",
		"Turn On the Entry Gate LED",
		"Turn On the EntryGate LED",
	};

	public static final String[] TurnOffEntryGateLED = {
		"Turn Off Entry Gate LED",
		"Turn Off EntryGate LED",
		"Turn Off the Entry Gate LED",
		"Turn Off the EntryGate LED",
	};

	public static final String[] TurnRedExitGateLED = {
		"Turn red Exit Gate LED",
		"Turn red ExitGate LED",
		"Turn red the Exit Gate LED",
		"Turn red the ExitGate LED",
		"Turn Off Exit Gate LED",
		"Turn Off ExitGate LED",
		"Turn Off the Exit Gate LED",
		"Turn Off the ExitGate LED",
	};

	public static final String[] TurnGreenExitGateLED = {
		"Turn green Exit Gate LED",
		"Turn green ExitGate LED",
		"Turn green the Exit Gate LED",
		"Turn green the ExitGate LED",
		"Turn on Exit Gate LED",
		"Turn on ExitGate LED",
		"Turn on the Exit Gate LED",
		"Turn on the ExitGate LED",
	};

	public static final String[] TurnOnSlotLED = {
		"Turn On Slot 1 LED",
		"Turn On Slot 2 LED",
		"Turn On Slot 3 LED",
		"Turn On Slot 4 LED",
		"Turn On Slot 5 LED",
	};

	public static final String[] TurnOffSlotLED = {
		"Turn Off Slot 1 LED",
		"Turn Off Slot 2 LED",
		"Turn Off Slot 3 LED",
		"Turn Off Slot 4 LED",
		"Turn Off Slot 5 LED",
	};

	public static final String[] SuccessAuthetication = {
		"Success Authentication",
		"Success Login"
	};
	
	public static final String[] FailAuthetication = {
		"Fail Authentication",
		"Fail Login"
	};
}
